import type { NextPage } from "next";
import { AuthLoginModule } from "src/components/modules/AuthLoginModule";

const AuthRegister: NextPage = () => <AuthLoginModule />;

export default AuthRegister;
